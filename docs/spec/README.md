# SNS Battle - 全体設計書

## 目次

- [プロジェクト概要](#プロジェクト概要)
- [デザインコンセプト](#デザインコンセプト)
  - [カラーパレット](#カラーパレット)
  - [デザイン原則](#デザイン原則)
- [機能一覧](#機能一覧)
  - [認証機能](#認証機能)
  - [配信機能（Live Streaming）](#配信機能live-streaming)
  - [マッチング機能（1対1ビデオ通話）](#マッチング機能1対1ビデオ通話)
  - [SNS Battle 機能](#sns-battle-機能)
  - [共通機能](#共通機能)
- [設計ドキュメント一覧](#設計ドキュメント一覧)
- [実装フェーズ](#実装フェーズ)
- [将来構想](#将来構想)
  - [課金・ショップシステム](#課金ショップシステム)
  - [MBTI・会話アシスト](#mbti会話アシスト)
  - [スタンプ・エフェクト拡張](#スタンプエフェクト拡張)

---

## プロジェクト概要

SNS Battle は、ライブ配信・1対1ビデオ通話マッチング・バトル形式の対戦を組み合わせたリアルタイムソーシャルプラットフォームである。

**3つのメイン機能**:

1. **配信機能**: 1対多のライブ配信。コメント・スタンプによるインタラクション
2. **マッチング機能**: 1対1のビデオ通話。トークテーマ表示・リアクション共有による楽しいマッチング体験
3. **SNS Battle 機能**: 1対1の対戦を観客付きで実施。スタンプ対決・ターン制トーク

初期リリースは Web 版のみ。将来的に Mobile（Expo）対応予定。

---

## デザインコンセプト

**ダーク × パステルパープル × シアン**のグラデーション基調デザイン。深い夜空（`#000319`）をベースに、淡いラベンダーパープル（`#CBACF9`）とシアン（`#0EA5E9`）を主役にした"クール×ドリーミー"な世界観。

### カラーパレット

実装値は `apps/web/src/app/globals.css` の `@theme inline` ブロックに CSS 変数として定義する。Tailwind v4 の `@theme inline` 経由で `bg-primary` `text-cyan` などのユーティリティクラスから利用する。

#### Base / Surface

| 用途 | トークン名 | 値 | 説明 |
|------|-----------|-----|------|
| 背景（最深） | `--color-dark-base` | `#000319` | ページ全体の背景。深い夜空をイメージした濃紺 |
| 背景（サーフェス） | `--color-dark-surface` | `rgba(4, 7, 29, 1)` | カード・サイドバー・モーダルの背景 |
| 背景（浮上） | `--color-dark-elevated` | `rgba(12, 14, 35, 1)` | ホバー・アクティブ状態のサーフェス |
| ボーダー | `--color-dark-border` | `rgba(255, 255, 255, 0.1)` | カード区切り、入力フィールド枠 |

#### Primary（パープル）

| 用途 | トークン名 | 値 | 説明 |
|------|-----------|-----|------|
| プライマリ | `--color-primary` | `#CBACF9` | メインアクション、ブランドカラー、アクティブ状態 |
| プライマリ（ホバー） | `--color-primary-hover` | `#b48ef5` | ボタンホバー時の濃色 |
| プライマリ（淡） | `--color-primary-light` | `#E2CBFF` | バッジ背景、タグ |
| プライマリ（背景用） | `--color-primary-bg` | `rgba(203, 172, 249, 0.06)` | 選択中メニュー項目背景 |
| プライマリ（ボーダー） | `--color-primary-border` | `rgba(203, 172, 249, 0.3)` | アクティブ状態の枠線 |
| プライマリ（グロー） | `--color-primary-glow` | `rgba(203, 172, 249, 0.15)` | ボックスシャドウ用グロー |

#### Accent（シアン / ピンク）

| 用途 | トークン名 | 値 | 説明 |
|------|-----------|-----|------|
| シアン | `--color-cyan` | `#0EA5E9` | サブブランドカラー、対戦相手側スポットライト |
| シアン（淡） | `--color-cyan-light` | `#67E8F9` | グラデーション補色 |
| シアン（グロー） | `--color-cyan-glow` | `rgba(14, 165, 233, 0.15)` | グロー用 |
| アクセントピンク | `--color-accent-pink` | `#EC4899` | バトル相手側、盛り上げコメント、通知バッジ |
| ピンク（淡） | `--color-pink-light` | `#F9A8D4` | グラデーション |
| ピンク（グロー） | `--color-pink-glow` | `rgba(236, 72, 153, 0.15)` | グロー用 |

#### Status

| 用途 | トークン名 | 値 | 説明 |
|------|-----------|-----|------|
| 成功 | `--color-success` | `#22C55E` | ライブインジケーター、配信中ドット |
| 成功（グロー） | `--color-success-glow` | `rgba(34, 197, 94, 0.15)` | グロー用 |
| 警告 | `--color-warning` | `#FBBF24` | 残り時間警告（10秒以下） |
| エラー | `--color-error` | `#EF4444` | LIVE バッジ、終了ボタン、残り時間（5秒以下） |

#### Text

| 用途 | トークン名 | 値 | 説明 |
|------|-----------|-----|------|
| テキスト（メイン） | `--color-text-primary` | `#FFFFFF` | 見出し・本文 |
| テキスト（セカンダリ） | `--color-text-secondary` | `#BEC1DD` | 配信情報のサブテキスト |
| テキスト（淡） | `--color-text-muted` | `#C1C2D3` | プレースホルダー、説明文 |
| テキスト（無効） | `--color-text-disabled` | `#636369` | 無効状態、極小ヒント |

#### フォント

| 用途 | トークン名 | 値 |
|------|-----------|-----|
| サンセリフ | `--font-sans` | `var(--font-geist-sans)` |
| 等幅 | `--font-mono` | `var(--font-geist-mono)` |

`next/font/google` で `Geist` と `Geist_Mono` を読み込み、`<html>` の `className` に `--font-geist-sans` / `--font-geist-mono` 変数を注入する。

### グローバルスタイルクラス

`globals.css` で定義する再利用可能な装飾クラス。

| クラス名 | 用途 | 主な指定 |
|---------|------|----------|
| `.bg-grid-pattern` | 背景グリッド | 32px 間隔の格子線（`rgba(255,255,255,0.03)`） |
| `.bg-dot-pattern` | 背景ドット | 16px 間隔のパープルドット（`rgba(203,172,249,0.15)`） |
| `.glass-card` | グラスモーフィズムカード | 半透明グラデーション + `backdrop-filter: blur(16px) saturate(180%)` + 1px 白枠 |
| `.glow-border-purple` | パープルグロー枠 | `box-shadow` で外側 + 内側に淡いパープル発光 |
| `.glow-border-cyan` | シアングロー枠 | 同上シアン版（マッチング相手側ビデオ等） |
| `.glow-border-pink` | ピンクグロー枠 | 同上ピンク版（バトル相手側勝利演出等） |
| `.animate-shimmer` | グラデシマー | 横スライドする `200% 100%` 背景アニメ |

### キーフレームアニメーション

| 名称 | 用途 | 仕様 |
|------|------|------|
| `shimmer` | ナビバー CTA、グラデの動的背景 | 2秒 linear infinite。`background-position: 0 0 → -200% 0` |
| `timer-shrink` | テーマごとタイマーバー | テーマの `duration` 秒間 linear で `width: 100% → 0%` |

### デザイン原則

- **ダークファースト**: 全画面ダークモードベース。ライトモードは初期リリースでは非対応
- **グラデーションファースト**: CTA・見出し・インジケーターは `linear-gradient(135deg, #CBACF9 0%, #0EA5E9 50%, #CBACF9 100%)` などの3点グラデで構成。`bg-clip-text` でテキスト自体にグラデを適用するパターンを多用
- **グラスモーフィズム**: カード、トークテーマ表示、コントロールバーは半透明 + `backdrop-filter: blur(...)` を基本。境界には 1px の白透過枠を必ず付与
- **動的グロー演出**: スポットライト中のビデオ、ボタンホバー、選択中の選択肢などは `box-shadow` で内外グローを重ねる
- **アニメーション**: ページ遷移・状態遷移・要素出現は Framer Motion で滑らかに実装。spring 物理ベースを基本とし、`easeInOut` を併用
- **背景装飾**: 各ページに大きな blur オーブ（`blur(120-150px)`、低不透明度のパープル/シアン）を `pointer-events-none fixed` で配置し、奥行きを演出
- **コンパクトサイドバー**: 折りたたみ可能（240px / 68px）。アクティブ項目はパープル枠 + パープルグロー背景
- **ライブ感の演出**: LIVE バッジは赤グラデ背景 + 点滅ドット。視聴者数・スタンプ数はリアルタイム更新
- **バトル感の演出**: VS 画面は左右分割。中央に円形 VS バッジ。スタンプカウントは左右からせり出す対立プログレスバー（パープル vs ピンク）

---

## 機能一覧

### 認証機能

- Google OAuth でのログイン / サインアップ
- JWT トークンによるセッション管理（既存実装を活用）
- 将来対応: TikTok, X(Twitter), Instagram OAuth
- ユーザープロフィール（アバター、表示名、自己紹介）

### 配信機能（Live Streaming）

| 機能 | 詳細 |
|------|------|
| 配信開始 | ブラウザカメラ/マイクから直接配信（WHIP）。将来的に OBS 対応（RTMP） |
| カウントダウン | 配信開始前に 3, 2, 1 のカウントダウンアニメーション表示 |
| 視聴 | 配信中のストリームを他ユーザーがリアルタイム視聴 |
| コメント | 視聴者がテキストコメントを送信。配信画面にリアルタイム表示 |
| スタンプ | 視聴者がスタンプ（絵文字/カスタム画像）を送信。配信者画面にアニメーション表示 |
| 視聴者数表示 | 現在の視聴者数をリアルタイム表示 |
| 配信終了 | 配信者が手動で終了。視聴者に終了通知 |

### マッチング機能（1対1ビデオ通話）

| 機能 | 詳細 |
|------|------|
| マッチング待機 | ユーザーがマッチングキューに参加。相手が見つかるまで待機画面表示 |
| マッチング成立 | 2人が揃ったらマッチング成立通知 |
| カウントダウン | ビデオ通話開始前に 3, 2, 1 のカウントダウン表示 |
| ビデオ通話 | 1対1のリアルタイムビデオ通話（カメラ + マイク） |
| 時間制限 | 合計 10 分で自動終了。5 分経過後に「終了ボタン」が有効化 |
| トークテーマ表示 | 1 分ごとにマスターデータから取得したトークテーマを両者の画面に表示。例）お互い１分間ずつ自己紹介してください。好きなお酒を答えてください。直近の恋愛話をしてくださいなどです |
| 選択肢・印象ボタン | トークテーマに対する回答を選択肢ボタンで表示。両者が選択 |
| リアクション一致 | 同じ回答を選んだ場合、紙吹雪エフェクトを表示 |
| 残り時間表示 | 通話の残り時間をプログレスバーで常時表示 |
| マッチング終了 | 時間切れまたは手動終了。終了後に感想画面（任意） |
| マッチング後のメッセージ機能| 将来的にはマッチング終了後にお互いにgoodだったら、チャットメッセージが可能になる|

### SNS Battle 機能

| 機能 | 詳細 |
|------|------|
| バトル作成 | ユーザーがバトルルームを作成し、対戦相手を待機 |
| バトル参加 | 対戦相手がバトルルームに参加 |
| 観戦参加 | 他ユーザーが観客としてバトルを視聴 |
| カウントダウン | バトル開始前に 3, 2, 1 のカウントダウン表示 |
| ターン制トーク | 1 分ずつ交互にトーク。現在のターンを画面に表示。マイクのミュート切替で制御 |
| バトルトークテーマ | バトル用のトークテーマを両者と観客の画面に表示 |
| スタンプ対決 | 観客がスタンプを送信。各プレイヤーへのスタンプ数をリアルタイム集計・表示 |
| 勝敗判定 | スタンプ数の多い方が勝利。結果画面を表示 |
| 観客コメント | 観客がコメントを送信。画面にリアルタイム表示 |

### 共通機能

| 機能 | 詳細 |
|------|------|
| カウントダウン | 3, 2, 1 + 「START!」のフルスクリーンオーバーレイアニメーション |
| スタンプシステム | 定義済みスタンプ一覧から選択して送信。画面上をフロートするアニメーション |
| ユーザープロフィール | アバター、表示名、自己紹介、年齢（生年月日から自動計算）、性別（男性/女性/その他）の表示・編集。将来: MBTI、居住地域 |
| 通知 | マッチング成立、バトル招待などのリアルタイム通知 |

---

## 設計ドキュメント一覧

### 横断設計

| ドキュメント | ファイル | 内容 |
|-------------|---------|------|
| 技術選定 | [tech-selection.md](./tech-selection.md) | 配信技術の比較検討（5候補）、LiveKit 詳細ガイド、全技術スタック選定理由 |
| インフラ設計 | [infrastructure.md](./infrastructure.md) | AWS 構成図、LiveKit Cloud 設計、WebSocket 不要の根拠、マッチングキュー設計 |

### 機能別設計（DB + API + UI + 仕様を各機能ごとにまとめて記載）

ステータスの定義: **設計中** = 設計書のみ作成済み / **実装中** = コードを実装中 / **完了** = 全 step が実装済み・テスト通過。`design-feature` skill で新機能を作るたびにこの表を更新する。

| ドキュメント | ステータス | ファイル | 内容 |
|-------------|---------|---------|------|
| 共通基盤 | 実装中 | [common/README.md](./common/README.md) | 共有テーブル（users, follows, blocks, items + item_scopes + stamp_details / effect_details / boost_details, user_inventory, coin_transactions, talk_themes）、認証/ユーザー/アイテム/課金 API、共通 UI コンポーネント（Phase 2: AppShell / Navbar / Sidebar / LiveBadge / VideoChatOverlay / CountdownOverlay / TimerBar / ConfettiEffect、step1-8 作成済） |
| 認証機能 | 実装中 | [auth/README.md](./auth/README.md) | Google OAuth フロー、JWT 管理、オンボーディング、将来のプロバイダー拡張（step1-5 作成済） |
| プロフィール機能 | 実装中 | [profile/README.md](./profile/README.md) | ユーザープロフィール（年齢・性別・MBTI）、マッチングフィルタ設定、オンボーディング（Phase 3: DB拡張 / GET・PUT・onboarding API / /onboarding・/profile/[id]・/profile/edit、step1-7 作成済） |
| 配信機能（1対多） | 設計中 | [streaming/README.md](./streaming/README.md) | streams / stream_comments / stream_stamps テーブル、配信 API、LiveKit Ingress / Webhook、視聴ページ UI |
| マッチング機能（1対1） | 設計中（step0-12 作成済） | [matching/README.md](./matching/README.md) | 3 サービス分離（apps/api + apps/matching-worker + Webhook）+ BullMQ delayed job ベースで 1 万人スケール対応。matching_queue / sessions / reactions テーブル、SSE、LiveKit Room + Data Channel、テーマ進行 worker、Webhook 後処理 worker、ロビー / セッション / 結果ページ |
| マッチングテーマ MBTI 最適化 | 設計中（step1-2 作成済） | [matching-theme-mbti/README.md](./matching-theme-mbti/README.md) | `talk_themes` に `target_score_min/max` を追加し、worker の `buildThemeSchedule` をペアの MBTI 相性スコアでフィルタ。スコア未設定ペアは既存のランダム挙動を維持 |
| バトル機能（1対1+観客） | 設計中 | [battle/README.md](./battle/README.md) | battle_rooms / battle_stamps / battle_comments テーブル、バトル API、ターン制トーク、スタンプ対決（Redis）、勝敗判定 |
| ソーシャル機能 | 設計中 | [social/README.md](./social/README.md) | フォロー / ブロック、ホームフィード、検索 |

---

## 実装フェーズ

| Phase | 名称 | 内容 | 依存 |
|-------|------|------|------|
| **Spec1** | **マッチング + プロフィール** | 認証（Google OAuth）、プロフィール（年齢・性別）、オンボーディング、マッチングキュー + ビデオ通話 + トークテーマ + スポットライト + 盛り上げコメント + マッチング中スタンプ送信、LiveKit セットアップ。実装の前提として `stamp_masters` を `items` 系へ統合する DB migration を Phase 3.5 で先行実施（[common/step9](./common/step9-db-migrate-stamp-to-items.md) 参照） | - |
| Spec2 | MBTI・会話アシスト | MBTI プロフィール設定、マッチング相性表示、トークテーマ最適化、会話アシスト | Spec1 |
| Spec3 | ライブ配信 | 配信開始/終了、コメント、スタンプ（無料）、視聴者数表示 | Spec1 |
| Spec4 | SNS Battle | バトルルーム + ターン制トーク + スタンプ対決 + 勝敗判定 | Spec1 |
| Spec5 | ソーシャル | フォロー/ブロック + ホームフィード + 検索 | Spec1〜4 |
| Spec6 | 課金・ショップ | コイン購入（IAP/Stripe）、有料スタンプ/エフェクト購入、ショップ UI | Spec3〜4 |
| Spec7 | マッチング後リレーション | マッチング終了 → 双方向 Good → 「チャットしようよ」「また話そうよ」のリクエスト送受信 → 承諾で DM / 再マッチを開放（[matching/README.md - マッチング後のリレーション形成](./matching/README.md#マッチング後のリレーション形成将来フェーズ) 参照） | Spec3 / Spec4 / DM 機能 |

---

## 将来構想

### 課金・ショップシステム

拡張性を重視し、全アイテムを `items` 親テーブル + 種別ごとの `*_details` テーブルで管理する Class Table Inheritance パターンを採用。新カテゴリ追加時に既存テーブルへの影響なし。

- **コイン**: アプリ内通貨。App Store / Google Play の IAP、または Stripe で購入
- **アイテム種別**: STAMP / EFFECT / BOOST（マッチング優先券等）/ DECORATION（プロフィール装飾）/ SUBSCRIPTION
- **使用シーン**: 1アイテムを複数シーン（MATCHING / BATTLE / STREAMING / PROFILE）で利用可能。`item_scopes` 多対多で表現
- **スタンプショップ**: type=STAMP を scope（マッチング向け / バトル向け / 配信向け）でフィルタして閲覧・購入
- **エフェクトショップ**: type=EFFECT。マッチング・バトル中に使える視覚エフェクト
- **所持品管理**: `user_inventory` で `quantity`（消費型対応）と `expires_at`（サブスク対応）を管理
- **取引履歴**: `coin_transactions` で全取引を記録。将来 `created_at` の range partitioning でスケール
- DB 設計は `docs/spec/common/README.md` に定義済み（items, item_scopes, stamp_details, effect_details, boost_details, user_inventory, coin_transactions）

### MBTI・会話アシスト

- **MBTI 設定**: プロフィールで16タイプから選択（INTJ, ENFP 等）
- **相性表示**: マッチング時に相手との MBTI 相性スコアを表示
- **会話アシスト**: MBTI 相性に基づいてトークテーマを最適化、相手への質問提案を自動生成
- **マッチング優先**: 相性の良い相手を優先的にマッチング（オプション）

### スタンプ・エフェクト拡張

- **アニメーション種別**: FLOAT（浮遊）/ BOUNCE（跳ねる）/ EXPLODE（爆発）/ SHAKE（振動）
- **マッチング中のスタンプ**: ビデオ通話中に相手画面にスタンプを送信、アニメーション付きで表示。Data Channel `matching:stamp` 経由（仕様は `docs/spec/matching/README.md` の「スタンプ送信」セクション参照）
- **複数シーン横断スタンプ**: 同一スタンプを `item_scopes` で MATCHING / BATTLE / STREAMING に紐付けて共通利用
- **エフェクト発動**: 選択肢の回答一致時やフリートーク中にエフェクトを発動
- **限定スタンプ**: 期間限定・イベント限定のプレミアムスタンプ（`items.is_premium=true` + `user_inventory.expires_at` で期間管理）
